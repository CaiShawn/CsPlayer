# CsPlayer v0.1.6 设计文档（排期中）

范围来自 `archived/v0.1.5_FEEDBACK.md` 的体验反馈（「已修复（2026-09）」一节除外，均已完成）。
基线见 `archived/V0.1.5_DESIGN.md`（多账号凭证库，已实现）与 `archived/V0.1.1_DESIGN.md` ~ `archived/V0.1.4_DESIGN.md`。

**状态：本文档为排期与设计稿（未实现），任务全部为 ☐ 待完成。** 实施中的决策微调统一补记到 §12。

> 版本路线：v0.1.5 多账号凭证库（已完成）→ **v0.1.6 体验优化：交互快赢 + 性能 + 首页 + 自定义背景**（本版）→ v0.1.7 唱片墙展示 + 分享卡片（核心，延后）→ v0.1.8 下载与格式转换（核心，延后）。
> 本版定位为**反馈驱动的体验打磨版**，无单一巨型主题；核心功能（唱片墙展示、分享卡片、下载）延后单独成版——v0.1.7 设计大纲见 `V0.1.7_DESIGN.md`，v0.1.8 预研见 §11.2。

## 1. 目标

1. **高频交互顺手**：左上角回首页、播放队列点空白收起、音量 UI 微调——改动小、感知强，最先落地。
2. **「我喜欢」/ 音乐库加载提速**：消除明显等待感，冷加载有可量化改善（目标见 §4.3）。
3. **首页成为像样的入口**：补齐模块、给出个人数据摘要与最近播放，但**不引入推荐流**（README 产品原则）。
4. **自定义背景图片**：本地图片 + 不透明度 / 模糊度 / 缩放 / 移动，可持久化、可一键关闭。

> 分享（反馈明确为**卡片式**、非链接、无二维码）与唱片墙展示合并至 v0.1.7，见 `V0.1.7_DESIGN.md`。

## 2. 排期总览（全部反馈项归宿）

| # | 反馈项 | 类别 | 优先级 | 归属版本 | 阶段 | 预估 |
|---|--------|------|--------|----------|------|------|
| 1 | 点击左上角时回到首页 | UI/UX | **P0** | v0.1.6 | S1-1 | 0.5 人日 |
| 2 | 播放队列展开时点击左侧空白自动收起 | UI/UX | **P0** | v0.1.6 | S1-2 | 0.5 人日 |
| 3 | 音量 UI 微调 | UI/UX | P1 | v0.1.6 | S1-3 | 0.5 人日 |
| 4 | 「我喜欢」/ 音乐库加载速度慢 | 性能 | **P0** | v0.1.6 | S0-1、S2 | 2 人日 |
| 5 | 优化首页 | UI/UX | P1 | v0.1.6 | S3 | 1.5 人日 |
| 6 | 分享功能（单曲、专辑、歌单，**卡片式、无二维码**） | 功能 | 延后 | **v0.1.7** | 见 §11.1 | 2–3 人日 |
| 7 | 自定义背景图片（不透明度 / 模糊 / 缩放移动） | 个性化 | P2 | v0.1.6 | S4 | 2.5 人日 |
| 8 | **唱片架展示功能**（唱片墙） | 功能（**核心**） | 延后 | **v0.1.7** | 见 §11.1 | 3–5 人日 |
| 9 | **下载并自动转换格式**（[ncmdump](https://github.com/taurusxin/ncmdump)） | 功能（**核心**） | 延后 | **v0.1.8** | 见 §11.2 | 5–8 人日 |

**排期原则**：

1. 核心功能（#6 分享卡片、#8 唱片墙展示、#9 下载）**体量大、独立成版**，整体后移：#6 与 #8 共享封面视觉链路、合并至 v0.1.7；#9 技术不确定性最大，**预研任务（§11.2）提前与 v0.1.6 并行**。
2. v0.1.6 内部按「快赢（S1）→ 性能（S2）→ 首页（S3）→ 个性化（S4）」推进：先交付感知最强、风险最低的改动，复杂度最高的背景图放最后，全程可随时发版。
3. 里程碑（按每周 4–5 人日节奏估算）：

| 里程碑 | 内容 | 累计人日 | 出口条件 |
|--------|------|----------|----------|
| M0 | S0 度量/存储实验 + S1 交互快赢 | ~3 | 快赢三项可用；性能基线、背景存储结论回填本文档 |
| M1 | S2 性能优化 | ~5 | 加载指标达 §4.3 目标 |
| M2 | S3 首页 | ~6.5 | 首页新布局、最近播放可用 |
| M3 | S4 自定义背景 | ~9 | 背景图全流程可用、默认关闭零回归 |
| M4 | S5 回归发版 0.1.6 | ~10 | §10 验收清单全勾 |

> 若需压缩到单周版本：可裁剪 S3-2 最近播放，其余不动。

## 3. 背景与现状

| 反馈 | 现状 | 位置 | 问题 |
|------|------|------|------|
| 左上角回首页 | TopBar 品牌区（`CsPlayer` + 副标题）是纯展示 `div` | `components/layout/TopBar.tsx` | 点击无响应，回首页只能靠侧栏 |
| 队列点空白收起 | `QueuePanel` 为右侧 `fixed` 浮层，仅 ✕ / 队列按钮可收起 | `components/player/QueuePanel.tsx` | 没有外部点击收起，高频操作绕远路 |
| 音量 UI | emoji 静音钮 + 原生 `input[type=range] h-1 w-20` | `components/player/Controls.tsx` `VolumeControl` | 与自绘 `ProgressBar` 视觉不统一、命中区过小、无滚轮/百分比反馈 |
| 我喜欢/音乐库慢 | `/like`、`/library` 首屏等 `Promise.all` 全量返回后一次性渲染；`/api/user/likes` 走上游「歌单详情」长响应；JSON 无压缩 | `pages/LikePage.tsx`、`pages/LibraryPage.tsx`、`stores/likesStore.ts`、`routers/song.py` | 空白 Loading 期长、二次进入仍重新拉全量、响应体大 |
| 首页 | `HomePage` 3 张静态卡片（我喜欢 / 听歌排行榜 / 唱片架），无「我的歌单」入口 | `pages/HomePage.tsx` | 入口不全、信息量低、模块名与侧栏文案不统一（听歌排行榜 vs 自听榜） |
| 分享（卡片式） | 无图片卡片生成/导出能力；右键菜单仅有「复制网易云网页版链接」（`copyLink`，链接形态，非本诉求） | — | 反馈明确分享为**卡片**形式（非链接、无二维码）→ 整体移至 v0.1.7（§11.1） |
| 自定义背景 | 全局固定实色底（`bg-neutral-950`）+ 主题色 | `stores/settingsStore.ts`、全局样式 | 无背景图能力；偏好持久化链路（`csplayer:prefs:*`）现成可复用 |

## 4. 方案对比与决策（按功能）

### 4.1 队列点击空白收起（#2）

| 候选 | 说明 | 结论 |
|------|------|------|
| a. 全屏半透明遮罩，点遮罩收起 | 判定简单、不会误触 | ❌ 首次点击被拦截，队列开着时无法直接操作页面，反而变慢 |
| **b. 面板外 `pointerdown` 即收起（决策）** | `document` 监听，命中在面板 ref 内则忽略；配合 `Esc` | ✅ 不拦截页面交互，抽屉式浮层的标准行为 |
| c. 仅当点击「页面空白处」（`e.target === 容器`）收起 | 最贴字面需求 | ❌ 命中判定随 DOM 嵌套脆弱，点到卡片/文字就不收起，行为不可预期 |

**决策：b。** 点击面板外**任意**位置（含左侧空白）收起队列；点击面板内不收起；`Esc` 同样收起。副作用（点歌曲行会顺带收起队列）为可接受的抽屉语义。右键菜单弹出的场景下，菜单点击不触发收起（命中判定排除 `ContextMenu` 根节点）。

### 4.2 音量 UI 微调（#3）

| 候选 | 说明 | 结论 |
|------|------|------|
| a. hover 展开式滑条（网易云式，默认只留图标） | 视觉省空间 | 备选记录：窄屏有价值，本版不做（展开动效 + 命中区复杂度不匹配「微调」诉求） |
| **b. 常驻滑条视觉统一 + 交互打磨（决策）** | 与 `ProgressBar` 共用滑轨/滑块视觉 token | ✅ 改动集中在一个组件，直击「微调空间」 |
| c. 弹出式音量面板 | 信息可扩展 | ❌ 过重 |

**决策：b**，具体微调项：

1. 自绘滑条与 `ProgressBar` 同款（同滑轨高度、滑块 hover 放大、accent 配色），替代原生 `range`；
2. 命中区从 `h-1` 提升到 `h-4`（视觉不变、好点好拖）；
3. 拖动 / 悬停显示百分比气泡；静音钮显示三态图标（静音 / 低 / 高），点击切静音并**记住静音前音量**（恢复准确）；
4. 悬停滚轮微调 ±5%（`preventDefault` 防页面滚动），键盘 `↑/↓` ±5%（聚焦时）；
5. 音量记忆沿用 v0.1.2 规则不变（`csplayer:prefs:volume`）。

### 4.3 性能：我喜欢 / 音乐库加载（#4）

先度量再动手（S0-1），候选按「收益 / 风险」排序：

| 候选 | 说明 | 结论 |
|------|------|------|
| a. 前端 SWR + 骨架屏 | 二次进入立即显示上次数据、后台刷新；首屏出骨架而非整页 Loading | ✅ 做（感知提升最大、零协议改动） |
| b. 后端上游并发 + 字段裁剪 | `library_service` 链路内串步改 `asyncio.gather`；响应只保留前端用到的字段 | ✅ 做 |
| c. GZip 中间件 | likes 千级曲目 JSON 可观 | ✅ 做（**排除 `/api/stream`**，避免二次压缩音频流） |
| d. 后端内存 TTL 缓存（likes/liked-ids） | 收益视上游耗时占比 | ⏸ 视 S0-1 结论决定；风险：`toggle` 红心后失效一致性，需失效钩子 |

**决策：a + b + c 进本版，d 挂起。** 量化目标（S0-1 记录基线后回填）：

- 二次进入 `/like`、`/library`：**缓存即时可见**（<100ms 出内容，后台静默刷新）；
- 冷加载：接口 P95 较基线**下降 ≥40%**，或首屏可交互 <1.5s（局域网部署），二者取先达标者。

### 4.4 优化首页（#5）

| 候选 | 说明 | 结论 |
|------|------|------|
| a. 最小修补 | 只补「我的歌单」入口、统一模块文案 | 不够，反馈明确要求「优化」 |
| **b. 导航 + 个人数据摘要 + 最近播放（决策）** | 欢迎条（头像/昵称）+ 4 模块入口 + 概览数字 + 最近播放 10 条 | ✅ 全部来自个人数据与本地记录，符合「无推荐流」原则 |
| c. 门户化（新碟速递、排行榜推荐、每日推荐） | 信息丰富 | ❌ 直接违反 README「无广告、无推荐流」产品定位 |

**决策：b**。首页结构：

```text
首页
├─ 欢迎条：头像 + 昵称 + 一句副标题
├─ 概览：我喜欢 N 首 · 我的歌单 N 个 · 收藏专辑 N 张（复用现有接口，异步补数、不阻塞首屏）
├─ 模块入口 ×4：我喜欢 / 我的歌单（音乐库）/ 自听榜 / 唱片架（文案与侧栏统一）
└─ 最近播放：本地记录 10 条（点击续播，右键沿用歌曲右键菜单）
```

- 最近播放为**新的本地偏好** `csplayer:prefs:recentPlays`（新工具 `utils/recentPlays.ts`，与 `recentSearch.ts` 同风格）：歌曲**开始播放**时记录、同曲去重置顶、上限 10、可一键清空；**不上传、不跨设备**。
- 概览数字：`likesStore` 已有 tracks、`libraryApi.playlists/albums` 已有 total，**零后端改动**；懒加载失败则隐藏该数字，不影响进入。

### 4.5 自定义背景图片（#7）

| 维度 | 候选 | 结论 |
|------|------|------|
| 存储 | a. 压缩后 dataURL 存 localStorage（`csplayer:prefs:background` 独立 key） | ✅ 与既有偏好链路一致、实现最简；前置 canvas 压缩（最长边 ≤1920、JPEG q0.8、目标 ≤300KB），超限提示「图片过大」 |
| | b. IndexedDB 存原图 | 备选演进（多图轮播/原图需求出现时再迁），本版不引入新存储面 |
| | c. `URL.createObjectURL` 不持久化 | ❌ 刷新即失效，与「个性化设置」预期不符 |
| 内容层可见性 | a. 应用底色改半透明表面 token（`--surface-*` 带 alpha），开关控制 | ✅ **「背景启用」时**才切半透明，**默认关闭 = 现状实色，零回归** |
| | b. 全量改造 UI 为半透明 | ❌ 波及全部主题、回归面过大 |
| 移动交互 | 设置卡内预览框拖拽平移 + 双击复位 + 数值滑杆 | ✅ 不污染主界面交互 |

**决策：** 独立设置节「背景」（`SettingSection id="background"`，排「主题外观」之后），控制项：选择/移除图片、不透明度 0–100%（默认 40）、模糊 0–40px（默认 0）、缩放 100–300%（默认 100）、位置拖拽（x/y 持久化，双击复位）、开关「背景启用」。

- 渲染：`BackgroundLayer`（`fixed inset-0`，置于内容层之下），`<img>` 走 `transform: translate(x,y) scale(s)` + `filter: blur(px)` + `opacity`；平移缩放只动 transform，避免重排。
- 性能兜底（S4-3 验收）：模糊 ≥24px 时在低端机不应导致播放卡顿/滚动掉帧，超标则把大模糊改为**压缩阶段 canvas 预模糊**（运行时只做 ≤24px）。
- 持久化：与 `contextMenu` 相同的独立 key 模式（`csplayer:prefs:background`），不进 `PREFS_KEY` 主信封（避免大字段进 backup 轮换）；「恢复默认设置」与「清除本地数据」**都**清除该项（属偏好、非凭证）。
- 隐私：图片仅存本机浏览器，不上传后端（说明文案沿用 v0.1.2 风格写在设置卡上）。

## 5. 功能范围

### 5.1 前端改动清单

| 文件 | 改动 |
|------|------|
| `src/components/layout/TopBar.tsx` | 品牌区可点击 → `/home`（当前页则回顶），补 hover/`cursor-pointer`/`aria-label` |
| `src/components/player/QueuePanel.tsx` | 外部点击 + `Esc` 收起（§4.1 b） |
| `src/components/player/Controls.tsx` | `VolumeControl` 微调（§4.2） |
| `src/pages/HomePage.tsx` | 首页重构（§4.4） |
| `src/utils/recentPlays.ts`（新） | 最近播放本地记录（读写/去重/清空） |
| `src/stores/playerStore.ts` | 歌曲开始播放时写最近播放 |
| `src/stores/likesStore.ts`、`src/pages/LikePage.tsx`、`src/pages/LibraryPage.tsx` | SWR 缓存 + 骨架屏 + 数据源去重（§4.3 a） |
| `src/stores/settingsStore.ts` | 新增背景偏好（独立 key）与默认值 |
| `src/components/common/BackgroundLayer.tsx`（新） | 背景图层渲染 |
| `src/components/settings/BackgroundSettings.tsx`（新）+ `src/pages/SettingsPage.tsx` | 「背景」设置节 |
| 全局样式（`index.css` / 主题 token） | 半透明表面 token（§4.5），默认关闭 |
| `src/version.ts`、`frontend/package.json`、`backend/app/main.py` | 版本号 → 0.1.6（S5） |

### 5.2 后端改动清单

| 文件 | 变更 | 说明 |
|------|------|------|
| `services/library_service.py`、`services/music_service.py` | 串步改 `asyncio.gather`、响应字段裁剪 | §4.3 b |
| `app/main.py` | `GZipMiddleware`（阈值 1KB，排除 `/api/stream`）；版本号 | §4.3 c |
| 路由层 | **零改动** | 本版各需求均为纯前端能力 |

## 6. 关键时序

```text
打开 /like（二次进入，SWR）
  → 缓存 tracks 立即渲染（骨架 → 内容 <100ms）→ 后台 fetchTracks() 静默刷新 → 有 diff 才替换列表
  → 首次进入：骨架屏 → 并发请求 → 渲染；失败保留上次缓存 + 顶部错误条 + 重试

队列展开中点击页面空白
  document pointerdown → 命中不在 QueuePanel / ContextMenu 根内 → queueVisible = false
  面板内点击 / Esc 对应行为（不收起 / 收起）

启用背景图
  选择图片 → canvas 压缩（≤1920 / q0.8）→ 成功：写 csplayer:prefs:background + 立即生效
                                   → 超限：toast「图片过大，请换小图」
  关闭「背景启用」→ 半透明表面 token 关闭 → 界面回到实色（与 v0.1.5 视觉一致）
```

## 7. 非目标（本版明确不做）

1. **分享功能（卡片式）与唱片墙展示**（核心）→ v0.1.7（§11.1、`V0.1.7_DESIGN.md`）。
2. **下载并自动转换格式**（核心）→ v0.1.8（§11.2）。
3. **链接式分享**：反馈已明确分享是卡片、非链接，不做（既有「复制网易云网页版链接」右键条目保持现状）。
4. **分享音频文件 / 歌单打包下载**：版权红线，永久不做（§8）。
5. **推荐流 / 新碟速递 / 每日推荐**：与 README「无推荐流」定位冲突。
6. **背景多图轮播、在线图床、跟随歌曲封面**：先落地单图 + 手动控制。
7. **设置云同步 / 跨设备同步**（含最近播放、背景图）：偏好只存本机。
8. **悬停展开式音量条**（§4.2 a 备选）。

## 8. 风险与合规（记录在案）

| 风险 | 说明 | 处理 |
|------|------|------|
| 分享卡片合规 | 卡片含封面/标题，可能被二次传播 | 只输出图片卡片、不含音频文件；细节在 v0.1.7 设计讨论时确定 |
| 背景图隐私 | 用户照片类图片入库 | 仅存浏览器 localStorage、不上传后端、设置卡明示、可一键移除 |
| localStorage 容量 | 背景图 dataURL 可能挤爆 5MB 配额 | 压缩到 ≤300KB；写入 try/catch 静默失败并提示「存储空间不足」；超限时引导换图（IndexedDB 演进见 §4.5） |
| blur 性能 | 大模糊值固定层重绘开销 | §4.5 预模糊兜底；验收含低端场景走查 |
| GZip 误伤流式 | 音频流二次压缩 | 中间件排除 `/api/stream` |
| SWR 数据陈旧 | 缓存先显 + 静默刷新存在短暂旧数据 | 红心等写操作后立即失效本地缓存；切账号（`dataVersion+1`）时清空缓存重拉 |

## 9. 实施计划：分步分模块任务划分

> 按 S0 → S5 顺序推进；S1 三项可并行。☐ 为待完成项。每项完成后在验收要点后追加结论（体例同 v0.1.5）。

### S0 前置度量与存储实验 — 预期代码改动 ≈ 0

| # | 任务 | 内容 | 涉及文件 | 依赖 | 验收要点 |
|---|------|------|----------|------|----------|
| S0-1 ☐ | 性能基线 | 记录 `/api/user/likes`、`/api/user/playlists`、`/api/user/albums` 前后端耗时与响应体积（前端 Performance + 后端临时计时日志），定位瓶颈（上游 / 串行 / 体积），结论回填 §4.3 | `routers/song.py`、`services/library_service.py`（临时日志） | 无 | 有基线数字与瓶颈结论；决定 §4.3 d（TTL 缓存）做或不做 |
| S0-2 ☐ | 背景存储实验 | canvas 压缩 1080p/4K 截图 → dataURL 实测体积；localStorage 上限与写入失败行为 | 临时脚本（不入库） | 无 | 确认 ≤300KB 目标可行；不可行则 §4.5 升级为 IndexedDB 方案并回填 |

### S1 交互快赢 — UI

| # | 任务 | 内容 | 涉及文件 | 依赖 | 验收要点 |
|---|------|------|----------|------|----------|
| S1-1 ☐ | 左上角回首页 | 品牌区可点击跳 `/home`；已在首页则回顶；hover 反馈 + `aria-label="回到首页"` | `components/layout/TopBar.tsx` | 无 | 任意页面点左上角到首页；不误触发选中文字 |
| S1-2 ☐ | 队列点空白收起 | 面板外 `pointerdown` 收起 + `Esc`；排除面板内与右键菜单（§4.1） | `components/player/QueuePanel.tsx`、`stores/playerStore.ts` | 无 | 点左侧空白收起；点面板内不收起；右键菜单操作不误收起 |
| S1-3 ☐ | 音量 UI 微调 | §4.2 五项：统一样式、命中区、百分比气泡、滚轮/键盘微调、静音三态与音量记忆 | `components/player/Controls.tsx`（必要时抽公共滑条组件） | 无 | 与进度条视觉一致；滚轮不引起页面滚动；静音恢复到静音前音量 |

### S2 性能 — 后端 + 前端

| # | 任务 | 内容 | 涉及文件 | 依赖 | 验收要点 |
|---|------|------|----------|------|----------|
| S2-1 ☐ | 上游并发与裁剪 | `library_service` 串步 → `asyncio.gather`；响应字段裁剪到前端实际使用集 | `services/library_service.py`、`services/music_service.py` | S0-1 | P95 达 §4.3 目标；字段契约不变（前端零适配或显式适配） |
| S2-2 ☐ | GZip | `GZipMiddleware(minimum_size=1024)`，排除 `/api/stream` | `app/main.py` | 无 | likes 响应体积显著下降；音频流播放无回归 |
| S2-3 ☐ | SWR + 骨架 | likes/tracks 内存缓存 + localStorage 快照（可选）、骨架屏、错误重试、`dataVersion` 切换清缓存；`/like` `/library` 共用 `likesStore` 单一数据源 | `stores/likesStore.ts`、`pages/LikePage.tsx`、`pages/LibraryPage.tsx` | S2-1 | 二次进入即时可见；红心操作后缓存一致；切账号不串数据 |
| S2-4 ☐ | TTL 缓存（挂起项） | 仅当 S0-1 判定瓶颈在上游时启用：likes/liked-ids 短 TTL 内存缓存 + like 变更失效 | `services/library_service.py` | S0-1、S2-1 | 红心后读己之写一致；TTL 过期正常回落上游 |

### S3 首页 — UI + 前端状态

| # | 任务 | 内容 | 涉及文件 | 依赖 | 验收要点 |
|---|------|------|----------|------|----------|
| S3-1 ☐ | 首页重构 | §4.4 结构：欢迎条、概览数字（异步补数）、4 模块入口（文案与侧栏统一） | `pages/HomePage.tsx` | 无 | 入口齐全可点；接口失败时降级为纯入口、无报错弹窗 |
| S3-2 ☐ | 最近播放 | `utils/recentPlays.ts` + 开始播放时记录（去重置顶、上限 10、可清空）+ 首页区块 | `utils/recentPlays.ts`（新）、`stores/playerStore.ts`、`pages/HomePage.tsx` | S3-1 | 播放后出现在首页；点击续播；清空后刷新仍为空；不上传 |

### S4 自定义背景 — 前端

| # | 任务 | 内容 | 涉及文件 | 依赖 | 验收要点 |
|---|------|------|----------|------|----------|
| S4-1 ☐ | 背景偏好与图层 | `csplayer:prefs:background` 模型（启用/图/不透明度/模糊/缩放/x/y）；`BackgroundLayer` 渲染；损坏静默回默认 | `stores/settingsStore.ts`、`components/common/BackgroundLayer.tsx`（新）、`App.tsx`/`MainLayout.tsx` | S0-2 | 刷新后背景保持；关闭开关立即回到实色界面 |
| S4-2 ☐ | 设置 UI | 「背景」设置节：选图/移除、三个滑杆、预览框拖拽移动 + 双击复位、隐私说明；恢复默认/清除本地数据联动 | `components/settings/BackgroundSettings.tsx`（新）、`pages/SettingsPage.tsx` | S4-1 | 全控制项即时生效并持久化；超限/写入失败有明确提示 |
| S4-3 ☐ | 半透明表面 | `--surface-*` alpha token，「背景启用」时生效，默认关闭零回归；含模糊性能兜底（§4.5） | 全局样式、`BackgroundLayer.tsx` | S4-1 | 7 套主题下文字可读；默认状态与 v0.1.5 视觉一致；低端场景滚动/播放不掉帧 |

### S5 版本回归

| # | 任务 | 内容 | 依赖 | 验收要点 |
|---|------|------|------|----------|
| S5-1 ☐ | 全链路走查 | 按 §6 时序与 §10 清单逐条验证 | S0–S4 | 清单全勾 |
| S5-2 ☐ | 无回归 | 播放/队列/音质/歌词/搜索/右键菜单/v0.1.2 设置/v0.1.4–v0.1.5 凭证链路不受影响 | 全部 | 全部通过 |
| S5-3 ☐ | 发版收尾 | 版本号 → 0.1.6（`version.ts`、`package.json`、`app/main.py`）；README Features/Updates 更新 | S5-1、S5-2 | 三处版本一致；文档同步 |

## 10. 验收清单（☐ 待验收）

- [ ] 任意页面点击左上角品牌区回到首页；在首页点击则回到页面顶部。
- [ ] 播放队列展开时点击左侧空白（或按 Esc）自动收起；点击队列内部不收起。
- [ ] 音量条与进度条视觉一致、命中区易点；滚轮/键盘可微调；静音再取消回到静音前音量。
- [ ] 二次进入「我喜欢」「音乐库」立即看到上次内容（<100ms），后台刷新且红心状态一致。
- [ ] 冷加载 P95 较 S0-1 基线下降 ≥40%（或首屏可交互 <1.5s）；音频流播放无回归。
- [ ] 首页含 4 个模块入口（含我的歌单）、概览数字与最近播放；断网/接口失败时优雅降级。
- [ ] 最近播放只存本机、去重置顶、上限 10、可清空。
- [ ] 背景图可选/可移除，不透明度、模糊、缩放、移动均可调并即时生效，刷新后保持。
- [ ] 关闭「背景启用」后界面与 v0.1.5 视觉完全一致（默认状态零回归）；7 套主题下背景开启后文字均可读。
- [ ] 图片过大 / 存储写入失败有明确提示且应用不崩；「恢复默认设置」「清除本地数据」能清掉背景偏好。
- [ ] 版本号三处一致为 0.1.6；**版本回归**（S5-2）全部通过。

## 11. 延后需求预研（核心功能，v0.1.7 / v0.1.8）

### 11.1 v0.1.7 唱片墙展示 + 分享卡片（核心）

两项反馈合并成版：**唱片架展示**（唱片墙）与**分享**（卡片式，无二维码）——同属「封面视觉呈现」链路，共享视觉与导出组件，一次设计、一起交付。
**设计思路与待决问题见 `V0.1.7_DESIGN.md`；卡片与唱片墙的具体设计待下一轮讨论确定，此处不展开。**

### 11.2 v0.1.8 下载与自动转换格式（核心，全栈主题版）

反馈「**下载并自动转换格式：使用库 [ncmdump](https://github.com/taurusxin/ncmdump)**」。独立成版理由：跨语言依赖集成 + 后端新链路 + 版权合规边界 + 大文件/转码 UX，是本批**技术不确定性最大**的需求。

**预研要点（S0 级调研建议提前至 v0.1.6 期间并行）**：

1. **场景澄清（先决问题）**：`ncmdump` 解的是网易云 `.ncm` 加密缓存；而 API 直链拿到的一般是普通 mp3/flac。需澄清目标场景：(a) 下载 API 直链音频 + 统一转码格式；(b) 解密用户自备 `.ncm` 文件；(c) 两者都要。**结论直接决定集成路线**。
2. **集成路线候选**：
   - 后端 Python 直连：下载流 → `ffmpeg` 转码（依赖本机 ffmpeg，启动时探测并提示）；
   - `ncmdump` 以 CLI/库形式集成（C++/C# 实现，Windows 向；需评估跨平台替代（纯 Python ncm 解密实现或 WASM 移植））；
   - 前端本地解密（WASM）+ 浏览器下载，后端零改动（隐私最优，性能受限）。
3. **链路设计要点**：`GET /api/download/{song_id}?format=mp3|flac` 流式响应 + `Content-Disposition`；批量下载（歌单/专辑）需任务队列 + 进度反馈（zip 打包）；VIP 音质权限与「可播放 ≠ 可下载」的降级策略。
4. **合规红线（先定死再开发）**：仅限用户**有权收听**的内容供个人离线使用；不做分享/分发；README 增加使用声明。分批下载（整单）是否开放需单独决策——建议默认关闭、设置中显式开启。
5. **UX**：单曲右键「下载」+ 详情页批量入口；格式/音质默认值进设置；大文件下载中断续传与否（建议 v1 不做）。
- 预估 5–8 人日（若 ncmdump 路线受阻需替代方案，另加 2–4 人日）。

> 版本规模：v0.1.7 预估 5–8 人日（设计细化后回填 `V0.1.7_DESIGN.md`），v0.1.8 预估 5–8 人日（路线受阻另加 2–4 人日）。

## 12. 实施补记（预留）

实施中的决策微调统一记录于此，**与上文冲突处以本节为准**（体例同 v0.1.5 §10）。
